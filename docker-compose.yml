version: '3.6'
services:
  php:
    build:
      context: .
      dockerfile: ./docker/php/Dockerfile
    image: dfw-normalization/php
    container_name: dfw-normalization_php
    networks:
      - dfw-normalization
    volumes:
      - .:/app
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/php.ini
networks:
  dfw-normalization:
    name: dfw-normalization
